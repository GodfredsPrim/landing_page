[build]
  command = "pip install -r requirements.txt"
  functions = "api"
  publish = "."

[functions]
  directory = "api"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200